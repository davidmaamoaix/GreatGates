'''Database stuff.'''

def getOrders(username: str):
	'''
	Fetches the user's pending and finished orders from the database.

	Parameters:
		username: the user's student id

	Returns: (pending, finished)
		pending: list, orders that are pending
		finished: list, orders that are finished
	'''
	return [], []